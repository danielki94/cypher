const PostReducer = (posts, action) => {

    switch (action.type) {

        case "DELETE":
            return ;

        




        default:
            return posts;
    }
};

export default PostReducer;